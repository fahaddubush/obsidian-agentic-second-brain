# GitHub configuration

This folder contains public repository automation and contribution metadata.

GitHub Actions must remain read-only unless a future reviewed workflow explicitly requires write access. Never place credentials or private vault data in workflow files or logs.
