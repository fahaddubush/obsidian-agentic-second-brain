# Continuous integration workflows

`validate.yml` runs the repository's standard-library test and validation gate on Linux and Windows.

Workflow permissions are read-only. Changes to automation require the same review as changes to Codex hooks.
